interface:
  display_name: "Scan Project"
  short_description: "扫描项目结构、技术栈和关键模块"
  default_prompt: "Use $scan-project to inspect a project, identify its structure and stack, and prepare integration context."
policy:
  allow_implicit_invocation: true