interface:
  display_name: "Reverse API"
  short_description: "从现有代码逆向提取接口定义，生成带来源和置信度的 API 文档"
  default_prompt: "Use $reverse-api to reverse-engineer this backend codebase into a structured 20_API_SPEC.md with source references and confidence notes."
policy:
  allow_implicit_invocation: true