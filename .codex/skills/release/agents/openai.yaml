interface:
  display_name: "Release"
  short_description: "汇总发布所需信息并生成 release 说明"
  default_prompt: "Use $release to assemble release evidence, summarize changes for a feature and version, and produce a structured release note."
policy:
  allow_implicit_invocation: true