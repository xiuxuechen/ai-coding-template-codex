interface:
  display_name: "GUI Connect"
  short_description: "将当前终端连接到 GUI 控制台"
  default_prompt: "Use $gui-connect to register the current terminal as a GUI session and report the connection status."
policy:
  allow_implicit_invocation: true