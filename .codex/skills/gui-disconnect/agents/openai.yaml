interface:
  display_name: "GUI Disconnect"
  short_description: "断开当前终端的 GUI 连接"
  default_prompt: "Use $gui-disconnect to remove the current terminal from GUI sessions and confirm the disconnect state."
policy:
  allow_implicit_invocation: true