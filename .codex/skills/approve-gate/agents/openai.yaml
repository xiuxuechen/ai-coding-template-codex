interface:
  display_name: "Approve Gate"
  short_description: "校验并记录 Phase Gate 审批动作"
  default_prompt: "Use $approve-gate to validate gate prerequisites, record an approval decision, and update the gate status safely."
policy:
  allow_implicit_invocation: true