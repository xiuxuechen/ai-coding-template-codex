interface:
  display_name: "Expert Review"
  short_description: "执行独立第三方评审并给出结构化结论"
  default_prompt: "Use $expert-review to perform an independent review for a feature, phase, or target file and return a structured GO, REVISE, or BLOCK outcome."
policy:
  allow_implicit_invocation: true