interface:
  display_name: "Run Tests"
  short_description: "执行测试并整理结果、失败项与结论"
  default_prompt: "Use $run-tests to locate the right tests, execute them, and summarize pass or fail outcomes with actionable next steps."
policy:
  allow_implicit_invocation: true