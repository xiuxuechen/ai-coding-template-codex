# Codex.md

本文件是仓库级 Codex 指令入口。

## 读取顺序

1. `Codex_INIT.md`
2. `.codex/README.md`
3. `docs/codex-playbooks/README.md`
4. 与当前任务直接相关的 `.codex/commands/`、`.codex/subagents/`、`.codex/skill-specs/` 或 playbook

## 目标

使用 Codex-only 路径加载本框架的全部能力，不依赖任何历史 `.codex/` 结构。

## 能力承载

- 自动触发能力：`.codex/skills/`
- 命令型 workflow：`.codex/commands/`
- 编排型能力：`.codex/subagents/`
- 内部能力参考：`.codex/skill-specs/`
- 落地执行手册：`docs/codex-playbooks/`

## 代码规范约束

如果项目中存在 `docs/_foundation/_planning/06_CODE_STANDARDS.md`，则在编码、重构、代码评审、测试补齐和修复建议时，应优先将其作为项目级代码规范单一事实源。

执行原则：
- 优先遵循该文档，再结合现有代码风格落地
- feature 文档可以增加更严格规则，但不应与其冲突
- 若现有代码与该规范不一致，应先说明差异，再决定沿用旧模式或推动规范升级
- 涉及 Git 提交时，提交前先检查其中的提交约定，默认使用中文提交信息

## 执行权限策略

如果项目中存在 `docs/_foundation/_planning/07_EXECUTION_PERMISSION_POLICY.md`，则在申请提权、选择 `prefix_rule`、判断是否需要逐次确认、区分开发/测试/数据库操作风险时，应优先参考该文档。

执行原则：
- 优先收敛成脚本白名单或窄前缀授权，不做宽泛全局放权
- 本地开发与共享环境、生产环境必须分级处理
- destructive action 不应进入持久放权范围

## 工作流输出合规

如果当前请求触发了 `.codex/commands/*.md`、`SKILL.md`、playbook 或其他仓库内工作流文档，并且其中定义了任何面向用户可见的要求，包括但不限于：执行步骤、固定话术、欢迎语、输出顺序、建议输出结构、固定分块、示例输出或其他等效模板，则这些内容一律视为强约束，必须体现在最终输出中，而不只是参考。

凡执行步骤中要求“显示”“说明”“列出”“输出”“提示”给用户的内容，均属于最终答案的必需组成部分，不能仅在内部执行而不对用户展示。

除非工作流文档明确标注某项为“可选”，否则不得：
- 省略
- 调整顺序
- 改写成自由总结
- 用更简短的话替代固定话术
- 只完成内部动作而不按要求输出给用户

执行这类工作流时，最终回答前必须逐项自检：
1. 是否已覆盖所有面向用户可见的执行步骤
2. 是否保留要求的固定分块或标题
3. 是否遵守指定顺序
4. 缺失内容是否已明确标注为“无”或说明原因

若以上任一条件未满足，则视为该工作流未完成，不能提交最终答案。

## 编码安全

对文件进行读取、编辑或写回前，必须先确认文件编码格式；修改后的文件必须与原有内容编码保持一致，不得擅自改变编码。

- 未确认编码前，不得整文件重写或覆盖原文件。
