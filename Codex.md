# Codex.md

本文件是仓库级 Codex 指令入口。

## 读取顺序

1. `Codex_INIT.md`
2. `.codex/README.md`
3. `docs/codex-playbooks/README.md`
4. 与当前任务直接相关的 `.codex/commands/`、`.codex/subagents/`、`.codex/skill-specs/` 或 playbook

## 目标

使用 Codex-only 路径加载本框架的全部能力，不依赖任何历史 `.codex/` 结构。

## 能力承载

- 自动触发能力：`.codex/skills/`
- 命令型 workflow：`.codex/commands/`
- 编排型能力：`.codex/subagents/`
- 内部能力参考：`.codex/skill-specs/`
- 落地执行手册：`docs/codex-playbooks/`

## 代码规范约束

如果项目中存在 `docs/_foundation/_planning/06_CODE_STANDARDS.md`，则在编码、重构、代码评审、测试补齐和修复建议时，应优先将其作为项目级代码规范单一事实源。

执行原则：
- 优先遵循该文档，再结合现有代码风格落地
- feature 文档可以增加更严格规则，但不应与其冲突
- 若现有代码与该规范不一致，应先说明差异，再决定沿用旧模式或推动规范升级
- 涉及 Git 提交时，提交前先检查其中的提交约定，默认使用中文提交信息

## 执行权限策略

如果项目中存在 `docs/_foundation/_planning/07_EXECUTION_PERMISSION_POLICY.md`，则在申请提权、选择 `prefix_rule`、判断是否需要逐次确认、区分开发/测试/数据库操作风险时，应优先参考该文档。

执行原则：
- 优先收敛成脚本白名单或窄前缀授权，不做宽泛全局放权
- 本地开发与共享环境、生产环境必须分级处理
- destructive action 不应进入持久放权范围

## 指令与技能输出对齐

如果当前请求触发了 `.codex/commands/*.md`、skill、playbook 或其他仓库内工作流文档，并且其中提供了明确的“示例输出”“建议输出结构”“固定分块”或等效模板，则这些内容应视为强约束，而不只是参考。

执行原则：
- 优先按示例输出的区块顺序、标题命名、分界线、列表结构、收尾提示进行响应。
- 可以补充与当前上下文直接相关的信息，但不应删除、合并或改写模板中的核心区块。
- 如果模板存在，而响应又属于 command 或 skill 型触发，则格式对齐优先于自然语言式总结。
- 只有在用户明确要求“换成更自然的说法”“不要模板化输出”时，才可以偏离示例版式。
- 如果由于真实上下文缺失、命令未执行或风险限制而无法完全对齐模板，必须先明确指出缺口，再给出尽量贴近模板的输出。

## 编码安全

读取或更新仓库文件时，应尽量保持文件原有编码，不得引入乱码、混合编码或因终端显示问题导致的错误改写。

执行原则：
- 编辑包含中文或其他非 ASCII 内容的文件前，先确认文件编码，并确保读写链路能够保持该编码。
- 如果当前终端显示为乱码，不应直接依据渲染结果判断原文内容；优先使用保守、可验证的方式修改文件。
- 在未确认编码安全前，不要整文件重写；优先做最小化、局部化修改。
- 对 command、skill、playbook 等文档的输出与落盘内容，不得出现由编码不一致引起的乱码。
- 如果当前无法安全确认编码，应先明确说明限制，再避免进行高风险改写。